<%= form_with(model: fame) do |form| %>
  <% if fame.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fame.errors.count, "error") %> prohibited this fame from being saved:</h2>

      <ul>
        <% fame.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :famenum, style: "display: block" %>
    <%= form.number_field :famenum %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
